numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
pyyaml>=6.0
h5py>=3.9
tqdm>=4.65
jupyter>=1.0
ipykernel>=6.25

# SNN side — partner picks one; both work with the same data interface
snntorch>=0.7
torch>=2.1

# NLB dataset tooling
nlb-tools>=0.0.4
dandi>=0.59
